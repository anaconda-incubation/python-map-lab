# Mobile redesign: implementation and validation

September 10, 2026 · Candidate branch: `mobile-redesign` · Baseline: `504362d` on `main`

The candidate is a separate worktree. The main checkout and published site have not changed. The local production preview is at **http://127.0.0.1:4177/**.

## What changed

The opening screen now puts a usable map ahead of the reading. Learn and Experiment share a single map, with compact selectors and a consistent question → observation → explanation → optional Python sequence. Phones have a dedicated Map/Python workspace, an expanded map focus view, and a Layers sheet. Desktop retains a broad map beside the explanation and an optional full-width view.

Default maps are generated by executing the canonical lesson Python in advance. Their geometry, exact numerical sample layout, thumbnails, and textures are versioned assets. Reading and selecting examples do not start Python. Opening the editor loads CodeMirror and prepares Pyodide; arbitrary edits still execute real NumPy and redraw the map.

Light, Dark, and System appearance work across the page, map, labels, editor, and dialogs. Scrolling is native. Animated projection transitions remain independently controllable.

## Measured layout comparison

These measurements describe the initial redesign commit `5afee2d`. The subsequent phone-width refinement is recorded below.

CSS pixels, beginning of the first canvas in the initial Globe view. Both layouts were inspected at the same viewport sizes. These are desktop browser responsive viewports, not physical devices.

| Viewport | Original canvas top | Candidate canvas top | Original canvas | Candidate canvas |
| --- | ---: | ---: | --- | --- |
| 320×740 | 1,220 | 264 | 278×300 | 294×303 |
| 360×800 | 1,183 | 232 | 318×300 | 334×328 |
| 390×844 | 1,073 | 232 | 348×300 | 364×346 |
| 430×932 | 1,031 | 232 | 388×300 | 404×382 |
| 768×1024 | 901 | 232 | 705×360 | 742×400 |
| 844×390 | 806 | 230 | 774×360 | 818×280 |
| 1440×900 | 638 | 344 | 669×358 | 814×495 |

No page-level horizontal overflow was found at these sizes. At 390×844, expanded mode increased the canvas from 364×346 to 390×708. At 844×390 landscape, the final focus view provides an 844×339 canvas, with its exit overlaid in the corner. The original portrait expansion had reduced its height from 300 to 248. The initial map starts slightly below the plan's 180–220px aspiration because controls retain 44px targets; it remains visible in the first phone viewport.

## Performance evidence

The comparison used built static sites on the same machine and in-app browser. Main was built unchanged; `scripts/profile-baseline.mjs` made an isolated temporary copy with a local measurement overlay. No application changes were made to main for measurement.

- **Initial reader resource transfer:** about **7.09MB → 1.48MB**, roughly 79% less in this local test. These are Resource Timing totals for requests visible to the top document, excluding the HTML and child-worker downloads. The baseline also downloaded Python resources inside its worker; those are not counted here. Vite's local handling/compression of raw GeoJSON can differ from Cloudflare, so this is not a claim of a measured 79% production reduction.
- **Reader startup:** no initial CodeMirror, PythonPanel, KaTeX, or Pyodide-worker request in the candidate. The baseline eagerly loaded the editor, mathematics, and worker.
- **Renderers:** two canvases in the original; one active renderer in the candidate, including after repeated mode changes and graphics recovery.
- **Final candidate warm reloads at 390×844:** first interactive frames occurred **402, 383, and 397ms after navigation**. Within those totals, map initialization took 66, 48, and 63ms; map fetch/decode took 23, 19, and 20ms. The measured LCP entries were 96, 60, and 72ms, with no observed long tasks in those three initial-load samples. LCP is not a measure of completion of a WebGL scene.
- **Baseline warm reloads at 390×844:** the old readiness flag appeared at 700 and 720ms; its first measured navigation was 953ms. That flag is a slightly different readiness signal from the candidate's actual first rendered frame. These are observations, not a statistically controlled device benchmark.
- **Animations:** a Globe → Mercator transition measured 899ms including endpoint loading, retaining the intended approximately 850ms morph. Reduced motion settles without the animated interval. Rapid selection back to the current projection was checked against the displayed endpoint.
- **Idle behavior:** settled frame counts stopped advancing; the existing demand-driven renderer is preserved. Hidden/offscreen rendering is paused. This does not claim a measured battery improvement.

The diagnostic `cumulativeLayoutShift` is a simple sum of observed shifts, not the official session-window CLS calculation. Its interaction entry is not an INP estimator. No field Core Web Vitals score, network/CPU-throttled result, or physical-phone frame-rate result is claimed.

## Completion against the 35-point plan

| # | Implementation and review outcome |
| --- | --- |
| 1 | Compact opening screen and immediately available genuine map image; seven viewport measurements above. |
| 2 | Small Learn/Experiment control, direct selector, step count and previous/next navigation. |
| 3 | Consistent question, observation prompt, explanation, and optional code; historical content and math preserved. |
| 4 | Native story reading; bounded desktop sticky map. Following the mobile review, the chooser stays pinned and the map pins only when it leaves sufficient reading room. Short screens retain normal map flow. Scrolling never runs code or selects projections. |
| 5 | Wide desktop map, approximately 62% of the workspace; constrained prose and explicit full-width mode. |
| 6 | Phone focus view uses dynamic viewport height and safe areas, Escape, focus containment, and return focus/scroll. Portrait expansion verified; landscape layout checked. |
| 7 | Default `pan-y` lets scrolling pass over the map; Explore opts into dragging. Keyboard rotation, zoom, Done, and Reset exercised. Physical touch remains a device check. |
| 8 | Map options now offers one explained distortion view, switches for names and coordinate lines, and optional coastline detail. Geography stays visible. Narrow/short viewports and keyboard controls were checked. |
| 9 | Existing prioritized, collision-aware geographic labels retained, excluded from redundant screen-reader output. Labels can be hidden; space-dependent placement is explained. |
| 10 | System/Light/Dark preference and early theme application implemented. Light and dark screenshots inspected, including editor, map, and errors. |
| 11 | Shorter introduction, consistent serif/UI/code typography, centered actual Anaconda logo, modest footer. Seven widths checked for overflow. |
| 12 | Lenis and GSAP scroll plumbing removed. Native wheel scrolling over the globe and keyboard map navigation exercised. Projection animation is separate. |
| 13 | All six experiments available in a labeled selector; selected equation remains unbroken in its own code area. Logarithm explanation is local to that recipe. |
| 14 | Cached city presets are derived from the exact displayed source. Dirty-source replacement asks Keep/Use; both paths tested. Meridian versus two-coordinate centering remains explicit. |
| 15 | Lazy phone editor, compact Run/Stop/Reset, Map/Python tabs, Back to lesson, comment shortcut and keyboard Run. Run becomes available even when the editor opens before the first map frame. |
| 16 | Touch Run reveals the result; keyboard Run preserves source focus. Last valid map survives failure. Captured source identifies the result even if editing continues during a run. |
| 17 | Per-lesson local drafts, safe preset URLs, Back/Forward, explicit source reset. Draft persistence and preset replacement checked; code stays out of URLs and analytics. |
| 18 | Prerendered HTML preview and introductory reading, lazy interactive map/editor/runtime/equations. Initial request audit confirms reader startup separation. |
| 19 | Shared active map stage; inactive lessons do not leave hidden renderers or editors mounted. |
| 20 | 29 canonical Python presets at two qualities plus two globes, with geometry, surface, distortion, labels, and preview assets. No speculative next-map prefetch is used: only requested assets consume bandwidth. |
| 21 | Exact invariant sample/probe arrays generated ahead of time. Numerical lookup avoids string keys and does not interpolate across custom cuts. |
| 22 | Overview uses Natural Earth 110m; Detailed uses 50m. Compatible buffers per quality; detail switching and both qualities' mathematical parity checked. |
| 23 | Source/data/schema fingerprints, hashed payloads, coalesced downloads, bounded decoded/custom caches, and immutable hosting headers. Build rejects stale generated assets. |
| 24 | Overview globe texture about 54KB; detailed about 229KB, replacing the initial 2.2MB JPEG request. Needed font weights/subsets only; editor/math fonts load on demand. |
| 25 | GPU morphs retained; stale selection guard and cancellation restore the previous map/ring. Rapid return to the current projection tested. |
| 26 | Honest execution stages, Stop, timeout, lazy restart, bounded output. A real infinite loop was stopped; timeout recovery and a subsequent successful keyboard Run were also exercised. |
| 27 | Abort signals across worker, geometry, and presentation; stale jobs cannot commit to another lesson. Result labels use the actual executed source. |
| 28 | Download and graphics failure produce a meaningful preview plus Retry. A map asset was temporarily withheld from the local build and restored; recovery succeeded. Graphics context loss/retry was also exercised. |
| 29 | Skip link, named sections, keyboard controls, pressed states, focused draft dialogs and expanded view, and textual overlay legends. DOM/accessibility-tree checks completed; physical screen-reader and 200% browser-zoom sessions remain external checks. |
| 30 | All four exported lesson notebooks executed in nteract, with real GeoPandas geography and saved map plots. Centered ring 19,000km succeeded; 19,001km was rejected. Comment shortcut preserves the doubled explanatory comments. |
| 31 | Shared map and draft-confirmation components, clearer workspace state/runtime/asset boundaries, removed obsolete parallel map/scroll code. Existing math and content remain authoritative. |
| 32 | Opt-in local diagnostics for fetch/decode, runtime, execution, result preparation, transitions, frame counts, and first-frame offset from navigation. No measurement transmission. |
| 33 | Reader request/renderer/idle budgets met in local checks. Numeric local measurements above; field CWV and throttled/physical-device goals are not certified. |
| 34 | Functional comparison, numerical parity, notebook runs, visual viewport matrix, cancellation, draft, preset, invalid-output, download, and context-recovery checks completed. Device-only limitations are listed below. |
| 35 | Worktree-only candidate and separate preview. Heap remains disabled awaiting approved IDs and Transcend configuration. No merge, push to main, or public deployment. |

## Mathematical and notebook verification

- `npm run check`: lint, **126 tests across 17 files**, TypeScript, Vite production build, and HTML prerender all passed.
- `node scripts/compare-baseline.mjs ../app`: all four lessons, six recipes, stories/history, equations, helpers, projection mathematics, and four full notebook exports match the accepted baseline.
- An independent nteract check reexecuted the canonical Python at 4,096 visible land vertices for each of 58 preset/quality pairs: **237,568 points**, maximum normalized coordinate error **2.9801881×10⁻⁸**. Both generated globe meshes also passed unit-sphere checks. See `notebook-checks/asset-parity.json` and `mobile-assets.ipynb`.
- All four lesson notebooks ran their dependency, projection, geography, drawing-helper, and final map cells successfully. Saved outputs were visually inspected for Mercator, Gall–Peters, Equal Earth, and AuthaGraph.
- The page's Equal Earth download produced a real notebook file. Its cells match the validated standalone export after trimming surrounding blank lines. Share copies the safe preset link, and the Sources drawer opens with the retained references and updated implementation credits.
- Regression coverage includes aligned binary array round trips, preset URL/source agreement, 110m Antarctica polar closure, seam-crossing triangle culling, and the existing projection/distortion/cancellation-related geometry suite.
- `npm audit`: **0 known vulnerabilities** at validation time, after compatible dependency updates.

Important defects caught in this pass: an inverted 110m Antarctica closure; lake triangles crossing projection cuts; a narrow-screen Layers panel outside the viewport; cancellation losing an existing distance ring; a late target winning after selecting back to the already displayed map; opening the editor before the map's first visible frame; and stale output text beneath a later failed run. These were corrected and the relevant browser paths rerun.

## Follow-up refinements

### Simpler map options

Replaced the row of layer pills with an **Explore distortion** selector: Plain map, Distortion circles, Area distortion, or Shape distortion. Only one distortion view is active at a time, making the visual and its explanation unambiguous. Land and water are always enabled. The lesson's Show/Hide distortion circles shortcut updates the same selection.

Place names and latitude/longitude lines use labeled switches with 48px rows. Coastline detail is an optional disclosure. Each distortion view has a short explanation, and active color views keep their key below the map after the panel closes. Close remains visible while scrolling a short panel; Escape closes the panel and restores focus without closing an expanded map.

Verified all four views, label and grid switching, the lesson shortcut, Standard/Detailed coastline changes, keyboard switch operation, focus return, and expanded-map dismissal. Inspected the panel at 320×740, 390×844, 844×390 landscape, and 1440×900, including Light and Dark. No horizontal overflow was observed. The 126-test, lint, TypeScript, and production build checks passed; no projection mathematics or notebooks changed.

### Mobile controls and map while reading

The Learn/Experiment control and projection chooser stay beneath the site header at widths up to 900px. The map stays beneath those controls when the remaining reading space is at least 240px and 32% of the visible viewport. This uses the actual map and control heights, rather than assuming all phones or projections are the same size. Tall maps and short landscape screens keep normal map flow; the chooser remains available. The map is not reduced to make it sticky.

Map/Python tabs stack beneath the chooser. In the Python pane the map is hidden, leaving the editor room to scroll. ResizeObserver and visual-viewport resize updates maintain the offsets; there is no scroll listener or additional rendering loop. Expanded mode remains a separate full-screen view and restores the previous scroll and focus on exit. Switching projections while reading returns to the new lesson's beginning.

Checked in responsive browser viewports:

| Viewport | Observed behavior |
| --- | --- |
| 320×740 | Mercator and controls pinned; 265px remained below the map. Globe released because its taller pane would crowd the reading. |
| 390×844 | Pinch the equator pinned, with 358px below it; Map/Python tabs and the editor scrolled correctly. |
| 476×1173 | Pinch the equator pinned, with about 633px of reading room; scrolling over the map moved the document normally. |
| 768×1024 | AuthaGraph and Gall–Peters pinned, leaving 382px below; expand/Escape preserved a 410px scroll position. |
| 844×390 | Chooser pinned; map scrolled away, keeping the lesson usable in landscape. |
| 1440×900 | Existing side-by-side desktop layout and sticky map retained. |

No horizontal document overflow was found in those viewports. Layers opened without moving the reading position, switching maps restored the lesson start, and Light/Dark sticky backgrounds were inspected. No browser warnings or errors were reported. The full lint, 126-test, TypeScript, and production build checks passed. These are browser viewport checks; physical touch and software-keyboard behavior remain device checks.

### Phone-width and dropdown refinement

Following the visual review, phone maps at viewport widths up to 600px now extend to the page edges. Portrait pane heights follow the projection's baked proportions, with a modest minimum for very small screens. At 476×1173, AuthaGraph is 476×206 and Mercator is 476×317, compared with the previous fixed 400px height. At 390×844, Mercator is 390×260. The camera uses a smaller gutter, and compact Mercator uses a 3:2 frame and fits the width, allowing polar edges to crop instead of forcing a square viewport. A visible note explains this; Expand restores the full projection. The globe and other projections retain their complete outlines, and globe exploration shares the same initial framing as Reset. Successful custom runs update the pane from their actual bounds.

Native dropdown options now set both their foreground and background colors explicitly. After Light → Dark, the inspected option colors changed from dark text on `#f7f8f3` to light text on `#0c1410`. Native popup pixels are not included in the browser screenshot API, so this verifies the applied styles and selection behavior rather than certifying every platform's native menu rendering.

Mercator, Equal Earth, Globe, expansion/return, narrow phone widths, landscape, and desktop were checked again. The full check suite still passes: 126 tests, lint, TypeScript, and production build. Projection formulas and notebook content did not change.

### Device and release limitations

The available browser interface supports responsive viewports, DOM/AX inspection, screenshots, keyboard/mouse interaction, and local app diagnostics. It does not provide physical iPhone Safari or Android Chrome, real touch/software-keyboard behavior, mobile GPU memory pressure, or network/CPU throttling. Those checks need representative devices before a broad launch. A full screen-reader session and field Web Vitals collection also remain unmeasured.

Overview deliberately uses coarser coastlines; the user can request Detailed. The generated collection is about 282MB on disk, but only the selected map is fetched. Individual files are below Cloudflare Pages' 25MiB limit (largest about 11.7MB); decoded high-detail geometry and active morph endpoints can still be substantial on low-memory devices. The cache limits are documented in `GENERATED_MAPS.md`.

The only production-build warning is Vite's advisory about the separately loaded Three.js chunk exceeding 500KB before compression (about 128KB gzip). It is not part of the initial reading shell. No unresolved test/build error remains.
